// Define a functional component named 'Loading'
export default function Loading() {
  // The component returns a simple message indicating a loading state
  return (
    // React Fragment shorthand <> is used to return the enclosed message
    <>Page is loading</>
  )
}
